import { ProductCatalog } from "./ProductCatalog";

export default function Products() {
    return (
        <section className="min-h-screen flex justify-center items-center" id="produtos">
            <ProductCatalog />
        </section>
    )
}